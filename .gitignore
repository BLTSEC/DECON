__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
*.swp
*.swo
.DS_Store

# Local environment and credential material
.env
.env.*
!.env.example
!.env.example.*
.envrc
*.pem
*.key
*.p12
*.pfx
credentials.json

# Kerberos credential cache files
*.ccache
*.ccach
*.kirbi

# Reversible DECON maps contain the original sensitive values
map.json
*.decon-map.json

# So do saved sessions and the audit log, if ever pointed inside a repo
audit.jsonl
*.audit.jsonl

# Personal scratch notes — machine- or workflow-specific, not part of the tool
*.local.md

# Local agent settings can contain machine paths and command allowlists
.claude/settings.local.json
.codex/
.agents/
